node_modules/
dist/
.env
.env.*
!.env.example
*.log
ledger.jsonl
weights.json
.DS_Store
.wallet/
*.key
.dev-refs.md
peak.json
positions.json
radar.json
daily.json
snapshot.json
baseline.json

# --- Terraform (OCI deployment) ---
# State holds every secret passed in as a variable, in plaintext. It belongs in
# the remote backend (see terraform/backend.tf), never in git.
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
backend.hcl
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
